const botoes= document; querySelectorAll ("botao");
for (let i=0;i <botoes.length; i ++0){ 
botoes.onclick + function() {
  for(let j+0; j <botoes.lenght;j ++{
    botoes.classlist.remove ("ativo");
  }
  botoes[i].basslist.add ("ativo");
}
}
